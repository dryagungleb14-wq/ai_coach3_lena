# Чек-лист для локального тестирования

## ✅ Что уже готово:
- [x] Все зависимости установлены
- [x] .env файл настроен
- [x] GEMINI_API_KEY установлен
- [x] DATABASE_URL настроен
- [x] База данных создана
- [x] Файлы загружаются

## 🔧 Что нужно проверить/доделать:

### 1. Запуск серверов
```bash
# Терминал 1 - Бэкенд
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# Терминал 2 - Фронтенд  
cd frontend
npm run dev
```

Или используйте скрипт:
```bash
./start.sh
```

### 2. Проверка работы API
- Откройте http://localhost:8000 - должно показать `{"message":"AI Coach API"}`
- Откройте http://localhost:8000/docs - документация API
- Откройте http://localhost:3000 - фронтенд

### 3. Тестирование функционала:
- [ ] Загрузка аудио файла
- [ ] Запуск анализа (расшифровка + оценка)
- [ ] Просмотр результатов
- [ ] Повторная проверка (retest)
- [ ] Экспорт в CSV
- [ ] История звонков

### 4. Возможные проблемы:
- **CORS ошибки** - проверьте, что бэкенд запущен на порту 8000
- **Failed to fetch** - проверьте, что оба сервера запущены
- **Whisper медленно работает** - это нормально, модель base скачивается при первом использовании (~150MB)
- **Gemini API ошибки** - проверьте GEMINI_API_KEY в backend/.env

### 5. Быстрая проверка:
```bash
# Проверка бэкенда
curl http://localhost:8000/api/calls

# Проверка фронтенда
curl http://localhost:3000
```

## 📝 Переменные окружения:

**backend/.env:**
```
DATABASE_URL=sqlite:///./ai_coach.db
GEMINI_API_KEY=ваш_ключ_здесь
```

**frontend/.env.local (опционально):**
```
NEXT_PUBLIC_API_URL=http://localhost:8000
```

